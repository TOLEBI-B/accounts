apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: accounts-ingress
spec:
  rules:
  - host: accounts.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: accounts-service
            port:
              number: 80